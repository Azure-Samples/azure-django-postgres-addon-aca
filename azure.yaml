# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-django-postgres-addon-aca
metadata:
    template: azure-django-postgres-addon-aca
infra:
    provider: "bicep"
services:
    web:
        project: src
        language: py
        module: web
        host: containerapp
